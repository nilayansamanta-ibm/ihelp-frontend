# React UI for iHelp Assistant
A React app for iHelp Assistant served as a static build with 'serve', to facilitate the users when trying to ask the assistant for any help.

## Setup
```bash
npm install
npm start